@{
    ViewBag.Title = "Đặt chỗ thành công";
    
}
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Tranchu</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100%;
        }

        .wrapper {
            min-height: 100vh; /* Chiều cao tối thiểu = 100% màn hình */
            display: flex;
            flex-direction: column;
            background-color: rgb(206, 152, 100, 0.85);
        }
        /* Header */
        header.top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 30px;
            background-color: rgba(164, 104, 46, 0.85);
            color: white;
            font-weight: bold;
            font-size: 18px;
            position: relative;
            z-index: 10;
        }

        header .logo {
            font-family: Georgia, serif;
            font-size: 24px;
            opacity: 0.7;
        }

        header nav a {
            color: white;
            text-decoration: none;
            margin-left: 25px;
            font-weight: 500;
        }

            header nav a:hover {
                text-decoration: underline;
            }

        header .icons {
            display: flex;
            align-items: center;
            gap: 15px;
        }

            header .icons .bell {
                width: 22px;
                height: 22px;
                cursor: pointer;
                filter: brightness(0) invert(1);
            }

            header .icons .profile-circle {
                width: 30px;
                height: 30px;
                border-radius: 50%;
                background-color: #cbb994;
                cursor: pointer;
            }

        /* Footer */
        footer {
            background-color: #2d1700;
            color: #e3ddd0;
            padding: 25px 60px;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            position: relative;
            z-index: 10;
            bottom: 0;
            width: 100%;
        }

            footer .footer-container {
                display: flex;
                justify-content: space-between;
                flex-wrap: wrap;
            }

            footer section {
                max-width: 300px;
                margin-bottom: 20px;
                line-height: 1.5;
            }

                footer section h4 {
                    margin-bottom: 10px;
                    font-weight: 700;
                }

                footer section a {
                    color: #cbb994;
                    text-decoration: none;
                    display: block;
                    margin-bottom: 6px;
                    font-style: italic;
                }

                    footer section a:hover {
                        text-decoration: underline;
                    }

            footer .address-info {
                font-style: italic;
            }

        .table-container {
            max-height: 520px; /* khoảng ~7 dòng với padding lớn */
            overflow-y: auto;
            /*            border: 1px solid #ccc;*/
            margin-top: 10px;
            padding: 12px 14px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 14px 18px;
            text-align: center;
            vertical-align: middle;
            min-width: 120px;
        }

        thead {
            background-color: #f2f2f2;
            font-weight: bold;
            position: sticky;
            top: 0;
            z-index: 1;
        }

        .btn-primary {
            background-color: #2d1700;
            color: #e3ddd0;
            padding: 12px 14px;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            font-size: 16px;
            text-decoration: none;
            border: none;
            border-radius: 5px;
        }

            .btn-primary:hover {
                background-color: peru;
                cursor: pointer;
            }

        .carousel-container {
            max-width: 800px;
            height: 400px;
            position: relative;
            overflow: hidden;
            margin: 30px auto;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
        }

        .carousel {
            position: relative;
            width: 100%;
            height: 400px; /* hoặc auto theo ảnh, hoặc % theo container cha */
            overflow: hidden;
        }

        .carousel-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: none;
        }

            .carousel-image.active {
                display: block;
                position: absolute;
                top: 0;
                left: 0;
            }

        .carousel-buttons {
            position: absolute;
            top: 50%;
            width: 100%;
            display: flex;
            justify-content: space-between;
            transform: translateY(-50%);
            z-index: 5;
        }

            .carousel-buttons button {
                background-color: rgba(0, 0, 0, 0.4);
                color: white;
                border: none;
                padding: 12px 18px;
                font-size: 24px;
                cursor: pointer;
                border-radius: 50%;
                transition: background-color 0.3s ease;
            }

                .carousel-buttons button:hover {
                    background-color: rgba(0, 0, 0, 0.7);
                }

        main {
            max-width: 80%;
            border: 2px solid black;
            align-items: center;
            margin-left: 10%;
            flex: 1;
            background-color: white;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <header class="top-bar">
            <div class="logo">THUQUAN.COM</div>
            <nav>
                <a href="@Url.Action("Danhsach", "Thietbi")">Trang chủ</a>
                <a href="#">Thủ tục</a>
                <a href="#">Liên hệ</a>
                <a href="@Url.Action("UserProfile", "Home")">Cá nhân</a>
            </nav>
            <div class="icons">
                <svg class="bell" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="white"><path d="M12 24c1.104 0 1.99-.896 1.99-2h-3.98c0 1.104.886 2 1.99 2zm6.364-6v-5c0-3.079-1.639-5.641-4.364-6.32v-.68c0-.826-.675-1.5-1.5-1.5s-1.5.674-1.5 1.5v.68c-2.725.679-4.364 3.241-4.364 6.32v5l-1.636 1.636v.364h15v-.364l-1.636-1.636z" /></svg>
                <div class="profile-circle"></div>
            </div>
        </header>
        <div class="container mt-5">
            <div class="alert alert-success text-center shadow p-4 rounded">
                <h2>✅ Đặt chỗ thành công!</h2>
                <p class="lead mt-3">Cảm ơn bạn đã sử dụng hệ thống đặt chỗ của chúng tôi.</p>
                <p>Mã đặt chỗ của bạn đã được ghi nhận. Vui lòng chờ xét duyệt hoặc theo dõi tình trạng tại mục "Lịch sử đặt chỗ".</p>

                <div class="mt-4">
                    <a href="@Url.Action("Danhsach", "Thietbi")" class="btn btn-primary me-2">Tiếp tục đặt chỗ</a>
                    <a href="@Url.Action("Danhsach", "Thietbi")" class="btn btn-secondary">Về trang chủ</a>
                </div>
            </div>
        </div>
</div>
</body>
</html>